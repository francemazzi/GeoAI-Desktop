# Runtime dependencies for bundled core QGIS plugins on macOS.
# Keep this list pinned: the package bundle must not depend on the packager's
# user site-packages or on whatever versions pip resolves on a later date.
packaging==26.2
Jinja2==3.1.6
MarkupSafe==3.0.3
psycopg2==2.9.12
# GDAL is intentionally absent from this lock file: its Python binding must be
# built from source at the exact version selected by the Strata CMake toolchain.
# The bootstrap receives that version from CMake (or gdal-config for direct
# developer invocations), preventing an ABI mismatch with vcpkg.
