# Runtime dependencies for bundled core QGIS plugins on macOS.
# Keep this list pinned: the package bundle must not depend on the packager's
# user site-packages or on whatever versions pip resolves on a later date.
packaging==26.2
Jinja2==3.1.6
MarkupSafe==3.0.3
psycopg2==2.9.12
# Required by QgsPythonUtils during every Python bootstrap. Build the bindings
# from source against the GDAL used for the Strata build; do not copy a
# Homebrew site-packages directory into the app bundle.
GDAL==3.13.1
